services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_USER: jobos
      POSTGRES_PASSWORD: jobos-local-only
      POSTGRES_DB: jobos
    ports:
      - "127.0.0.1:5432:5432"
    volumes:
      - jobos-postgres:/var/lib/postgresql/data
volumes:
  jobos-postgres:
