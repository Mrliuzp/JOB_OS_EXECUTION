# Architecture

The authoritative architecture is defined in `AI_JOB_OS_EXECUTION_SPEC.md`.

WP-001 establishes only the repository boundaries and executable entry points. Domain models, providers, workflows and browser automation are intentionally deferred to later work packages.
